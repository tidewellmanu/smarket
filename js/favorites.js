import {auth,db} from "./firebase.js";
import {collection,query,where,getDocs,doc,deleteDoc} from "https://www.gstatic.com/firebasejs/12.18.0/firebase-firestore.js";
import {formatPrice} from "./countries.js";import {escapeHtml,imageOrPlaceholder} from "./utils.js";
const fav=document.getElementById("favorites"), saved=document.getElementById("saved-searches");
if(fav){if(!auth.currentUser){fav.innerHTML="<div class=empty>Sign in to see favorites.</div>"}else{getDocs(query(collection(db,"favorites"),where("userId","==",auth.currentUser.uid))).then(async s=>{const cards=[];for(const d of s.docs){const l=await (await import("https://www.gstatic.com/firebasejs/12.18.0/firebase-firestore.js")).getDoc(doc(db,"listings",d.data().listingId));if(l.exists()){const x=l.data();cards.push(`<a class="listing-card" href="listing.html?id=${l.id}"><img class="thumb" src="${imageOrPlaceholder(x.coverImage,x.title)}" alt=""><div class=body><h3>${escapeHtml(x.title)}</h3><div class=price>${formatPrice(x.price,x.countryCode)}</div></div></a>`)}}fav.innerHTML=cards.join("")||"<div class=empty>No favorites yet.</div>"})}}
if(saved){if(!auth.currentUser){saved.innerHTML="<div class=empty>Sign in to see saved searches.</div>"}else{getDocs(query(collection(db,"savedSearches"),where("userId","==",auth.currentUser.uid))).then(s=>saved.innerHTML=s.docs.map(d=>`<div class="stat-card"><strong>${escapeHtml(d.data().name||"Saved search")}</strong><p>${escapeHtml(d.data().queryText||"")}</p></div>`).join("")||"<div class=empty>No saved searches.</div>")}}